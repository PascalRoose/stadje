# Neon Postgres connection string (constitution Technology Stack & Hosting; ADR 0003, 0005).
# Only used for the world_stats_daily table (data-model.md) — not required for playing the
# game locally; the puzzle itself uses static, repo-committed data/puzzle-cycle.json (ADR 0011).
DATABASE_URL=

## What & why

<!-- What does this change, and why? Link an issue if there is one. -->

## How was this tested?

<!-- pnpm test / pnpm build / manual steps -->

## Checklist

- [ ] `pnpm biome check .` passes
- [ ] `pnpm exec tsc --noEmit` passes
- [ ] `pnpm test` passes
- [ ] `pnpm build` succeeds
- [ ] Any game-rule change (hint tiers, selection, streaks) is reflected in
      [`.specify/memory/constitution.md`](../.specify/memory/constitution.md), not just the code
- [ ] Any real architectural/technical decision has an ADR under [`docs/adr/`](../docs/adr/)
